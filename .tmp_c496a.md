P3 (robustness): если в истории окажутся JSON-string arguments (legacy-хранилище), они будут молча заменены на {}. Сегодня рантайм порождает только dict (ai_coach_runtime.py:418), поэтому не блокирует. Предлагаю либо fallback json.loads для строк, либо комментарий, фиксирующий контракт dict-only.
