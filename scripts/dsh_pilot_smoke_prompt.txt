Read-only smoke для пилота DeepSeek Harness. Ни один файл менять нельзя.

Задача: прочитать два файла и ответить по фактам.

1. `tests/smoke/test_quality_bike_tss_delivery.py` — перечисли tuple `QUALITY_TEMPLATES` в том порядке, в котором он записан.
2. `docs/engineering_process_metrics.md` — в записи по PR #572 / issue #562 найди, сколько коммитов реализации там указано и какой диапазон назван рядом с этим числом.
3. В том же файле найди раздел Revisit Gate и приведи значение `n`, при котором записано решение, и что именно решено сохранить.

Ответь ровно тремя пунктами, каждый — одна строка. Если чего-то в файле нет, так и напиши: «не найдено». Не создавай, не изменяй и не удаляй файлы; не запускай команды, изменяющие рабочее дерево.
