# Показывать структуру Garmin-активности без Intervals.icu

Этот ExecPlan — живой документ. Разделы `Progress`, `Surprises & Discoveries`, `Decision Log` и `Outcomes & Retrospective` обновляются по мере работы. Документ ведётся в соответствии с `.agent/PLANS.md`.

## Purpose / Big Picture

После изменения пользователь, который подключил только Garmin, увидит в карточке активности круги или этапы сразу после синхронизации Garmin. Если позже станет доступна запись Intervals.icu, автоматически определённые интервалы появятся вторым отдельным блоком и не скроют круги. Проверить результат можно на странице `/activities`: у Garmin-активности вместо сообщения «Интервалы недоступны» появляется список кругов с длительностью, дистанцией и доступными показателями.

## Progress

- [x] (2026-08-13 08:13Z) Подтверждено текущее ограничение: карточка читает только структуру Intervals.icu, а сегодняшняя активность имеет только Garmin-связь.
- [x] (2026-08-13 08:13Z) Проверен реальный ответ Garmin только на чтение: `get_activity_splits` вернул `lapDTOs` с 7 кругами.
- [x] (2026-08-13 08:14Z) Добавлены падающие проверки нормализации Garmin, кэширования при синхронизации и приоритета Intervals.icu; исходное падение — отсутствующая `normalize_garmin_splits_payload`.
- [x] (2026-08-13 08:18Z) Реализован общий контракт структуры, сохранение Garmin-кругов и подпись источника в карточке.
- [x] (2026-08-13 08:24Z) Пройдена 71 связанная проверка, полный набор 1706 тестов / 1 пропуск среды, Next lint/build, compileall и браузерная приёмка реальной активности с 7 кругами.
- [x] (2026-08-13 08:40Z) После пользовательской проверки исправлен выявленный разрушающий приоритет: круги Garmin и интервалы Intervals.icu сохраняются и показываются одновременно.
- [x] (2026-08-13 08:44Z) Пройдены 72 связанные и 1707 полных проверок, Next lint/build, compileall и браузерная приёмка реальной карточки с 7 кругами Garmin и 5 интервалами Intervals.icu.

## Surprises & Discoveries

- Observation: установленный клиент Garmin уже предоставляет метод `get_activity_splits`, поэтому новый внешний протокол или зависимость не нужны.
  Evidence: реальный ответ активности `23958642824` содержит ключи `activityId`, `eventDTOs`, `lapDTOs`; в `lapDTOs` семь строк с длительностью, дистанцией, пульсом, мощностью и типом интенсивности.
- Observation: таблица `activity_intervals` уже хранит компактный JSON по каноническому идентификатору активности и подходит для обоих источников без изменения схемы SQLite.
  Evidence: `data/activity_store.py::save_activity_intervals` не связывает формат с конкретным поставщиком.

## Decision Log

- Decision: загружать Garmin-круги во время уже авторизованной синхронизации Garmin, а карточку оставлять локальной и быстрой.
  Rationale: это исключает повторный вход в Garmin при каждом открытии карточки и соответствует требованию ASR-PERF-1 не выполнять сетевые вызовы на обычном чтении страницы.
  Date/Author: 2026-08-13 / Codex.
- Decision: круги Garmin и интервалы Intervals.icu хранятся раздельно; автоматически определённые интервалы остаются входом сравнения плана с фактом, но не заменяют круги в карточке.
  Rationale: это разные представления одной тренировки: Garmin сохраняет фактические нажатия круга, а Intervals.icu аналитически определяет рабочие отрезки.
  Date/Author: 2026-08-13 / Codex.
- Decision: не загружать секундные потоки Garmin; использовать только ограниченный ответ кругов `get_activity_splits`.
  Rationale: кругов достаточно для карточки, а объём и время синхронизации остаются ограниченными.
  Date/Author: 2026-08-13 / Codex.

## Outcomes & Retrospective

Garmin стал самостоятельным источником структуры фактической активности.
Сегодняшняя пробежка `23958642824`, у которой нет связи Intervals.icu, показывает
в карточке 7 кругов с длительностью, дистанцией, мощностью, пульсом и типом
интенсивности. Intervals.icu сохраняет роль необязательного обогащения: его
непустой результат показывается отдельным блоком, не скрывая Garmin-круги.

Итоговые проверки после исправления: 72 связанных теста прошли; полный безопасный набор — 1707
тестов прошли, один ожидаемо пропущен из-за запрета локального сетевого порта;
Next.js lint и production build прошли; Python compileall прошёл; браузерная
карточка показывает 7 кругов Garmin и отдельные 5 интервалов Intervals.icu. Новых
зависимостей и изменений схемы SQLite нет.

## Context and Orientation

`data/garmin_client.py` оборачивает установленный клиент Garmin и уже используется синхронизацией. `services/sync.py::_sync_activities` нормализует и сохраняет активности, но сейчас не запрашивает их круги. `models/activity_intervals.py` приводит ответ Intervals.icu к компактной форме для карточки. `services/activity_intervals.py` читает и обновляет кэш структуры, но возвращает `None`, если у активности нет связи Intervals.icu. `api/routers/activities.py::get_activity_card` добавляет эту структуру в ответ, а `web/app/activities/page.tsx` показывает список.

Под «структурой» здесь понимаются фактические круги или этапы завершённой активности. Это не плановая структура тренировки. Под «кэшем» понимается локально сохранённый компактный JSON в SQLite, позволяющий открыть карточку без сети.

Затронуты ASR-PERF-3, ASR-REL-2 и ASR-MOD-3. Синхронизация должна оставаться инкрементальной; отказ дополнительного запроса кругов не должен терять основную активность; формат структуры расширяется обратно совместимо без обязательной миграции таблицы.

## Plan of Work

Сначала в `tests/smoke/test_activity_intervals.py` добавляются сценарии поведения: нормализация реального Garmin-формата, сохранение структуры для активности только из Garmin, сохранение старого кэша при ошибке и совместное хранение обоих источников. В тестах синхронизации фиксируется, что ошибка загрузки кругов не отменяет сохранение основной активности.

Затем в `data/garmin_client.py` добавляется безопасная обёртка `get_activity_splits`. В `models/activity_intervals.py` появляется `normalize_garmin_splits_payload`, которая переводит `lapDTOs` в существующие поля карточки, вычисляет последовательное временное смещение и отмечает источник. В `services/sync.py` после успешного сохранения активности круги запрашиваются и кэшируются только при отсутствии отдельно сохранённых Garmin-кругов; ошибка остаётся необязательным предупреждением и не меняет счётчики основных активностей. В `services/activity_intervals.py` локальный Garmin-кэш возвращается и без связи Intervals.icu; успешный ответ Intervals.icu добавляется рядом с ним.

Наконец, `web/lib/types.ts` и `web/app/activities/page.tsx` получают аддитивное поле источника и понятную подпись «Круги Garmin» или «Интервалы Intervals.icu». Сообщение об отсутствии данных остаётся честным для активности, где ни один источник не дал структуру.

## Concrete Steps

Рабочий каталог: `/Users/gregkisel/Developer/ai_trainer`.

Сначала запустить новый целевой тест до реализации и увидеть падение:

    ai_trainer_env/bin/python -m pytest tests/smoke/test_activity_intervals.py -q

После реализации повторить целевой набор, затем выполнить:

    ai_trainer_env/bin/python -m pytest tests/smoke/test_garmin_sync_service.py tests/smoke/test_m1_common_ingest.py tests/smoke/test_plan_vs_fact.py -q
    ai_trainer_env/bin/python -m pytest tests/smoke -q
    npm --prefix web run lint
    npm --prefix web run build

Для проверки реальных данных повторить Garmin-синхронизацию и открыть активность 13 августа 2026 года на `/activities`. Ожидается семь кругов и подпись источника Garmin.

## Validation and Acceptance

Дано: активность имеет только Garmin-связь и ответ Garmin содержит `lapDTOs`. Когда выполняется Garmin-синхронизация, тогда основная активность и компактная структура сохраняются; карточка возвращает непустой список кругов без Intervals.icu.

Дано: Garmin не вернул круги или запрос завершился ошибкой. Когда выполняется синхронизация, тогда основная активность всё равно сохраняется, прежний кэш не удаляется, а карточка остаётся работоспособной.

Дано: активность позже получает связь Intervals.icu. Когда карточка успешно загружает определённые Intervals.icu интервалы, тогда они появляются отдельным блоком рядом с Garmin-кругами. Если Intervals.icu временно недоступен, оба ранее сохранённых набора остаются видимыми.

Приёмка завершена, когда новые тесты сначала падают, затем проходят; весь безопасный набор тестов, проверка стиля и производственная сборка проходят; реальная сегодняшняя карточка показывает семь Garmin-кругов.

## Idempotence and Recovery

Повторная синхронизация безопасна: существующая структура не удаляется, а сохранение использует текущую операцию `INSERT ... ON CONFLICT DO UPDATE`. Основная активность сохраняется до необязательного обогащения кругами, поэтому сбой Garmin не требует отката базы. При необходимости изменение можно отменить обычным обратным коммитом; миграции схемы и разрушительных операций нет.

## Artifacts and Notes

Исходное подтверждение на реальных данных:

    activity_id: 23958642824
    provider links: garmin only
    Garmin split response: 7 lapDTOs
    local cached structure: absent

## Interfaces and Dependencies

В `data/garmin_client.py` должен существовать метод:

    GarminClient.get_activity_splits(activity_id: str) -> dict | None

В `models/activity_intervals.py` должна существовать функция:

    normalize_garmin_splits_payload(payload: Any) -> dict[str, Any]

Общий результат структуры остаётся объектом с `analyzed`, `intervals` и `groups`, получает `source` со значением `garmin` или `intervals` и необязательный отдельный список `garmin_laps`. Новые библиотеки не добавляются.
